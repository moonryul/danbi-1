using UnityEngine;

public class Discription : MonoBehaviour {


  // 캐릭터 부각 -> 필 라이트 ( 림 라이트의 반대 ) 카메라가 곧 라이트가 되어버림!
  // 하프 렘버트 업그레이드 버전 -> n dot l = (n dot l * offset) / (1 + offset) -> 한결 화사한 이미지 가능! 

  // 우버 쉐이더 -> 거대하게 짜놓고 다른곳에서 define으로 사용

}
